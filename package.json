{
  "name": "tradelistener",
  "version": "0.0.0-dev",
  "description": "Checks for trades and publishes events when a trade is done.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "tdaclient": "^1.5.1"
  }
}
